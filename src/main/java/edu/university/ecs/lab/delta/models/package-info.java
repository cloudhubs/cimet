/**
 * This package contains models used for representing changes between two commits in a microservice system.
 *
 * <p>It includes the a class to represent a single change and a class for an overall change 
 * in the IR (Intermediate Representation) from an old commit to a new commit. There is also a package that holds an enum to signify
 * the type of change made in each commit (i.e ADD, MODIFY, DELETE).</p>
 *
 */
package edu.university.ecs.lab.delta.models;
